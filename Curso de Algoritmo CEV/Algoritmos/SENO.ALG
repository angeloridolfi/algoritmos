algoritmo "seno"
var
   seno, angulo: real
   
inicio
   escreva("Digite um ângulo para ver seu seno:")
   leia(angulo)
   seno <- Sen(GraupRad(angulo))
   escreva("O seno de", angulo, " é", seno)

fimalgoritmo